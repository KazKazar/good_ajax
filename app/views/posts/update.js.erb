$('#myupdatepost_<%= @post.id %>').modal('hide'); //hide modal of update

$("#post_<%= @post.id %>").fadeOut(500, function(){// will happen only after the effect-fadeout is completed(timer=500)
  $("#post_<%= @post.id %>").after( "<%= j render @post %>" );
  $(this).remove();
});